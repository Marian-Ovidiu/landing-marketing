import { HeroMobile } from "@/components/hero/HeroMobile";

export const metadata = { title: "Concept C — Mobile, stato iniziale" };

export default function Page() {
  return <HeroMobile stage="initial" />;
}
