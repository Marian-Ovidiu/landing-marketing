import { HeroMobile } from "@/components/hero/HeroMobile";

export const metadata = { title: "Concept C — Mobile, stato finale" };

export default function Page() {
  return <HeroMobile stage="final" />;
}
