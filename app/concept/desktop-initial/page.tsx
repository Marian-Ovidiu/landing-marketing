import { HeroDesktop } from "@/components/hero/HeroDesktop";

export const metadata = { title: "Concept C — Desktop, stato iniziale" };

export default function Page() {
  return <HeroDesktop stage="initial" />;
}
