import { HeroDesktop } from "@/components/hero/HeroDesktop";

export const metadata = { title: "Concept C — Desktop, stato finale" };

export default function Page() {
  return <HeroDesktop stage="final" />;
}
